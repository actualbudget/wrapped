[build]
command = "yarn build"
publish = "dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

[build.environment]
NODE_VERSION = "18"

# Domain configuration: wrapped.actualbudget.org
# Set this in Netlify dashboard: Site settings > Domain management > Custom domains
